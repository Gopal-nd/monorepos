import React from "react";
import Admin from "@repo/ui/admin";
import Input from "@repo/ui/input";
const page = () => {
  return (
    <div>
      <Admin>Welcome to admin Page</Admin>
      <Input />
    </div>
  );
};

export default page;
